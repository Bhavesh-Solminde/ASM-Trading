import { TradeWorkspace } from "@/components/trade/TradeWorkspace";

// Accounts, assets and recent trades are loaded once by the platform layout.
export default function TradePage() {
  return <TradeWorkspace />;
}
